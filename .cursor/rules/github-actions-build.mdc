---
description: Prefer GitHub Actions for Linux backend binaries; download via 10811 proxy
alwaysApply: true
---

# GitHub Actions 编译与本机下载（server）

本机交叉编译 Linux backend（openssl-sys / vendored OpenSSL + Windows Perl）易失败；deploy-host（~4GB RAM）也不适合 release 编译。**默认用 GitHub Actions 产出 `artifact-keeper-linux-amd64`。**

## Workflow

- 文件：`.github/workflows/build-linux-binary.yml`
- 名称：`Build Release Binaries`
- 触发：`workflow_dispatch`，或 push 到 `feat/**` / `fix/**`
- 产物 artifact：`artifact-keeper-linux-amd64`（含裸二进制 + `.tar.gz` + sha256）
- **必须在 `registry.access.redhat.com/ubi9/ubi:9.8` 容器内编译**（与 `docker/Dockerfile.backend` / deploy-host 运行时一致）。在裸 `ubuntu-latest` 上编出会链接 glibc 2.38+，容器内报 `GLIBC_2.38/2.39 not found`。

CLI 侧同源策略见 `artifact-keeper-cli` 的 `.cursor/rules/github-actions-build.mdc`。

## 下载（必须走 10811）

```bash
export HTTPS_PROXY=http://127.0.0.1:10811
export HTTP_PROXY=http://127.0.0.1:10811
# 再 curl / gh run download Actions artifact
```

GitHub UI 上显示的 sha256 通常对应 **artifact zip**，不是解压后的 ELF。

## 部署 deploy-host

```bash
scp artifact-keeper-linux-amd64 deploy-host:/tmp/artifact-keeper
ssh deploy-host 'docker cp /tmp/artifact-keeper artifact-keeper-backend:/usr/local/bin/artifact-keeper && docker restart artifact-keeper-backend'
# 等 healthy：docker inspect --format "{{.State.Health.Status}}" artifact-keeper-backend
```

入口：`http://127.0.0.1:30080`（账号见运维笔记，勿把密码写进规则）。

## Agent 行为

1. ferry / 需要联调的后端改动 → push `feat/**`，等 Actions，再代理下载部署。
2. 不要在 Windows 上 `cargo zigbuild` 编 backend；不要在 deploy-host 上 `cargo build --release`。
